# Vela app environment (Expo inlines EXPO_PUBLIC_* at build time).
# For local development point these at the local Supabase stack. After `npm run db:start`
# (supabase start) at the repo root, read the values from `supabase status`:
#   API URL   -> EXPO_PUBLIC_SUPABASE_URL
#   anon key  -> EXPO_PUBLIC_SUPABASE_ANON_KEY
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key

# Dogfood-only Pro grant (AOD-75): uncomment to unlock the Pro gates in local dev. Only the exact
# value "pro" grants; eas.json sets it for `preview` builds and pins it to "off" for `production`,
# so it can never ship in a store build. See src/entitlements/devEntitlements.ts.
# EXPO_PUBLIC_DEV_ENTITLEMENTS=pro
