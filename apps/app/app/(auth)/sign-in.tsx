// The /sign-in route (app-ia.md §5 row 1). Thin: delegates to the src/ screen (so the Unistyles babel
// plugin covers it). The card is placed in the (auth) zone here; the form / sign-up interior is AOD-29.
export { SignIn as default } from '../../src/screens/SignIn';
