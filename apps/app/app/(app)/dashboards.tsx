// The /dashboards route (app-ia.md §5 row 6): the create / switch dashboard switcher. Presented as a modal
// route (options set in (app)/_layout.tsx). Thin: delegates to the src/ placeholder; the switcher affordance
// + multi-dashboard active-selection interior is AOD-27.
export { DashboardsScreen as default } from '../../src/screens/placeholders';
