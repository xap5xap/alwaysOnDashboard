// The /settings/account route (app-ia.md §5 row 11): the Account screen (sign out relocated here, deletion,
// manage subscription). Thin: delegates to the src/ screen (so the Unistyles babel plugin covers it).
export { Account as default } from '../../../src/screens/Account';
