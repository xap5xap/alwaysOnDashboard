// The /settings route: the settings home / secondary hub (app-ia.md §5 row 7). Thin: delegates to the src/
// screen (so the Unistyles babel plugin covers it). Moved here from app/settings.tsx (app-ia §9 move+extend);
// gains sibling routes themes.tsx + account.tsx. The Connections section interior stays AOD-28's.
export { Settings as default } from '../../../src/screens/Settings';
