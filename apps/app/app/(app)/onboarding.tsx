// The /onboarding route (app-ia.md §5 row 2): the first-run flow. Thin: delegates to the src/ placeholder;
// the guided stepper + connect-first-service interior is AOD-29. Reached from the gate when a signed-in
// user is not yet onboarded (useOnboarded seam).
export { OnboardingScreen as default } from '../../src/screens/placeholders';
